# JOBOS environment (copy to .env)
# Workspace root (MASTER/, PROFILES/, ...). Defaults to repo root.
JOBOS_ROOT=

# SQLAlchemy URL. Defaults to a local SQLite file at repo root.
# For Postgres (spec 49):
# JOBOS_DATABASE_URL=postgresql+psycopg://user:pass@host:5432/jobos
JOBOS_DATABASE_URL=
