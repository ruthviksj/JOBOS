services:
  - type: web
    name: jobos
    runtime: docker
    plan: free
    envVars:
      - key: JOBOS_ROOT
        value: /app
      - key: PORT
        value: "8000"
      - key: JOBOS_DATABASE_URL
        sync: false
        required: true
    healthCheckPath: /health
